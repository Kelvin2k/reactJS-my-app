import React, { Component } from "react";

export default class DemoEvent extends Component {
  render() {
    return (
      <div>
        <h2>Demo about events in react</h2>
        <button onClick={} className="btn btn-success">Click on me!</button>
      </div>
    );
  }
}
